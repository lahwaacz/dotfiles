# "#" or ";" at the start of a line makes it a comment.
[DEFAULT]
# gmail account address
email: kuba.klinkovsky@gmail.com
# get password from encrypted file, use gpg-agent to ask for password to decrypt the file
passwordeval: gpg --use-agent --quiet --batch -d ~/.mutt/password-gmail.gpg

# optional
cache_filename: ~/.cache/goobook_cache
cache_expiry_hours: 24
filter_groupless_contacts: no
